DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/your-webhook-id/your-webhook-token
DISCORD_TOKEN=

# Optional; default API host already sends browser-compatible headers.
MFL_API_BASE_URL=https://api.playmfl.com
# On Render, set this to an ATTACHED persistent disk mount (e.g. /var/data).
# A directory alone does not provide persistence. Locally the default is .
# DATA_DIR=/var/data
