{
  "name": "mfl-location-tracker-cloudflare",
  "private": true,
  "type": "module",
  "devDependencies": {
    "@cloudflare/workers-types": "latest",
    "@types/node": "^26.6.1",
    "tsx": "^4.23.13",
    "typescript": "latest",
    "wrangler": "4.135.0"
  },
  "scripts": {
    "test": "tsx --test tests/*.test.ts",
    "check": "wrangler types && tsc --noEmit",
    "deploy": "wrangler deploy"
  }
}
