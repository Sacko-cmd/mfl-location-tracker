services:
  - type: web
    name: mfl-location-tracker
    env: python
    plan: free
    rootDir: WalletWatcher
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    healthCheckPath: /health
    envVars:
      - key: DISCORD_WEBHOOK_URL
        sync: false
      - key: DISCORD_TOKEN
        sync: false
